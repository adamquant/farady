name: Release SA Tests

on:
  push:
    branches: [release-sa]
  pull_request:
    branches: [prod-sa]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.13
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]
    
    - name: Run unit tests
      run: pytest tests/ -v --ignore=tests/integration/test_lambda_smoke.py

  contract-and-e2e-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.13
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]
    
    - name: Run API contract tests
      run: pytest tests/integration/test_api_contract.py -v
    
    - name: Run E2E tests
      run: pytest tests/integration/test_e2e.py -v
